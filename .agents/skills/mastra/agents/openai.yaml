interface:
  display_name: "Mastra"
  short_description: "Repo-specific patterns and workflows for mastra"
  default_prompt: "Use the mastra repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true